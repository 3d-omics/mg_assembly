# reads: environment to preprocess reads before mag assembly
---
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - megahit=1.2.9
  - quast=5.2.0
  - bowtie2=2.5.1
  - samtools=1.17
